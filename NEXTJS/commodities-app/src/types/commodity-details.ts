export type CommodityDetails = {
    title: string;
    description: string;
    code: string;
    currentPrice: number;
    currency: string;
    frequency: string;
    lastUpdate: string;
    comparison: string;
    lastPrice: number;
};
